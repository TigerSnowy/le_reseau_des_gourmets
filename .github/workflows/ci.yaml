name: Le Réseau des Gourmets CI

on:
    workflow_dispatch:

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - run: |
                ls
                mkdir test
                cd test
                touch index.html
